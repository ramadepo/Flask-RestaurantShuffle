version: '3'
services:
  mysqldb:
    image: ramadepo/rsmysql:1.5
    build:
      context: ./mysql
      dockerfile: Dockerfile
    networks:
      - my-net
  my-rs:
    image: ramadepo/restaurant-shuffle:1.5
    build:
      context: ./restaurantshuffle
      dockerfile: Dockerfile
    networks:
      - my-net
  nginx:
    image: ramadepo/rsnginx:1.5
    build:
      context: ./nginx
      dockerfile: Dockerfile
    ports:
      - "80:80"
    networks:
      - my-net 

networks:
  my-net:
    driver: bridge